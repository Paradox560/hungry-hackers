/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: App Router has a different i18n approach than Pages Router.
  // We're using a custom context-based solution instead of route-based config
  // so we can remove the i18n configuration here
};

module.exports = nextConfig;
